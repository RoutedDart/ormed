name: ormed_mysql
description: MySQL / MariaDB adapter for the routed ORM driver interface.
version: 0.1.0-dev
publish_to: none
repository: https://github.com/routedart/routed
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  ormed:
  mysql_client_plus: ^0.1.2
  carbonized: ^0.1.0
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
  driver_tests:
