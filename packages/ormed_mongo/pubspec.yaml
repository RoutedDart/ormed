name: ormed_mongo
description: MongoDB adapter for the routed ORM driver interface.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
resolution: workspace
environment:
  sdk: ">=3.10.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  ormed:
  mongo_dart: ^0.10.5
  ormed_sqlite: ^0.1.0-dev

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
  driver_tests:
