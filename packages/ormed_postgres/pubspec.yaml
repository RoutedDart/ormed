name: ormed_postgres
description: PostgreSQL adapter for the routed ORM driver interface.
version: 0.1.0-dev
repository: https://github.com/RoutedDart/ormed
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  ormed: 0.1.0-dev
  postgres: ^3.5.9
  carbonized: ^0.1.0
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
  driver_tests:
