## Unreleased

- **Updated**: Example now imports generated registry from `lib/src/database/orm_registry.g.dart`.
- **Updated**: Migration registry examples now use `MigrationEntry.named(...)`.
- **Updated**: Environment lookups now use `OrmedEnvironment` helpers.
- **Updated**: Bumped `artisanal` to `^0.5.0`, `contextual_shelf` to `^0.4.0`, and the testing/storage dependencies to their current compatible releases.
- **Updated**: Requires Dart 3.12 or newer.

## 1.0.1

- **Updated**: Synchronized ormed changes.

## 1.0.0

- Initial version.
