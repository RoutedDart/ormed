## 0.1.0-dev

- Introduce `@OrmModel`, `@OrmField`, and `@OrmRelation` annotations.
- Add runtime primitives (`ModelDefinition`, `ModelRegistry`, `ValueCodecRegistry`).
- Provide source-gen builder that emits codecs + metadata into `*.orm.dart` parts.
- Add sample todo model, docs, and tests covering metadata + codec flows.
