/// Marker mixin that enables the generated factory helpers.
///
/// Apply `ModelFactoryCapable` to your model (directly or via a superclass)
/// when you want the generator to emit the factory helper class and register
/// the definition with the factory registry.
mixin ModelFactoryCapable {}
