name: ormed_examples
description: Runnable code examples for Ormed documentation
publish_to: none
resolution: workspace
environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  ormed:
  ormed_sqlite:

dev_dependencies:
  build_runner: ^2.10.4
  test: ^1.28.0
